---
- hosts: localhost
  become: true
  tasks:
  - name: install additional packages
    portage:
      package: "{{ item }}"
      state: present
      newuse: yes
      verbose: yes
    ignore_errors: True
    with_lines: cat packageAdditional//*| grep -v '#'
